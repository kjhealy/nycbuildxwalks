% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_longform.R
\name{build_longform_for_primary}
\alias{build_longform_for_primary}
\title{Build long-form crosswalk for a single primary geography}
\usage{
build_longform_for_primary(
  all_gdf,
  primary_id,
  other_ids,
  buffer_feet = -50,
  min_area = 100,
  epsilon = 1e-06,
  max_primaries = NULL
)
}
\arguments{
\item{all_gdf}{An sf object containing all boundaries, projected to
EPSG:2263 (NY State Plane, feet).}

\item{primary_id}{The geography ID to use as the primary (e.g., \code{"cd"}).}

\item{other_ids}{Character vector of target geography IDs.}

\item{buffer_feet}{Negative buffer (in feet) applied during intersection
to suppress edge artifacts. Default \code{-50}.}

\item{min_area}{Minimum intersection area in square feet. Default \code{100}.}

\item{epsilon}{Tiny area threshold to suppress numerical noise. Default
\code{1e-6}.}

\item{max_primaries}{Optional integer to limit the number of primary
features processed (useful for testing).}
}
\value{
A tibble with columns \code{primary_geo_id}, \code{primary_geo_name},
\code{other_geo_id}, \code{other_geo_name}, \code{primary_area_sqft},
\code{intersection_area_sqft}, and \code{pct_overlap}.
}
\description{
For each feature of the primary geography, computes the intersection area
with every feature of each target geography. Returns a tibble with one
row per significant pairwise intersection, including area and percentage
overlap.
}
