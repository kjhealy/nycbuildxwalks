% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_crosswalks.R
\name{build_crosswalks}
\alias{build_crosswalks}
\title{Build all crosswalk tables from boundary data}
\usage{
build_crosswalks(
  boundaries_path,
  run_dir,
  buffer_feet = -50,
  min_area_final = 100,
  epsilon = 1e-06,
  exclude_ids = "cc_upcoming",
  primary_only = NULL,
  targets = NULL,
  max_primaries = NULL
)
}
\arguments{
\item{boundaries_path}{Path to \code{all_boundaries.geojson}.}

\item{run_dir}{Output directory for crosswalk CSVs and metadata.}

\item{buffer_feet}{Negative buffer in feet for intersection de-noising.
Default \code{-50}.}

\item{min_area_final}{Minimum intersection area in square feet. Default
\code{100}.}

\item{epsilon}{Tiny area to suppress numerical noise. Default \code{1e-6}.}

\item{exclude_ids}{Character vector of geography IDs to exclude. Default
\code{"cc_upcoming"}.}

\item{primary_only}{Optional character vector to restrict which primary
geography IDs are processed.}

\item{targets}{Optional character vector to restrict target geography
IDs.}

\item{max_primaries}{Optional integer to limit features per primary
(for testing).}
}
\value{
The path to \code{run_dir} (invisibly).
}
\description{
Reads a combined boundaries GeoJSON file, reprojects to EPSG:2263
(NY State Plane, feet) for accurate area calculations, and builds
both long-form and wide-format crosswalk CSVs for each primary
geography.
}
