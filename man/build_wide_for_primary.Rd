% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_wide.R
\name{build_wide_for_primary}
\alias{build_wide_for_primary}
\title{Build wide-format crosswalk for a single primary geography}
\usage{
build_wide_for_primary(
  all_gdf,
  primary_id,
  other_ids,
  buffer_feet = -50,
  min_area = 100,
  epsilon = 1e-06,
  max_primaries = NULL
)
}
\arguments{
\item{all_gdf}{An sf object containing all boundaries, projected to
EPSG:2263 (NY State Plane, feet).}

\item{primary_id}{The geography ID to use as the primary (e.g., \code{"cd"}).}

\item{other_ids}{Character vector of target geography IDs.}

\item{buffer_feet}{Negative buffer (in feet) applied during intersection
to suppress edge artifacts. Default \code{-50}.}

\item{min_area}{Minimum intersection area in square feet. Default \code{100}.}

\item{epsilon}{Tiny area threshold to suppress numerical noise. Default
\code{1e-6}.}

\item{max_primaries}{Optional integer to limit the number of primary
features processed (useful for testing).}
}
\value{
A tibble with one row per primary feature. The first column is
named after \code{primary_id} and contains the primary feature names.
Remaining columns are named after each target geography ID and contain
semicolon-separated overlapping feature names.
}
\description{
For each feature of the primary geography, finds overlapping features
from all target geographies and returns a tibble with one row per primary
feature and one column per target geography (values are semicolon-separated
names of overlapping features).
}
