% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_run.R
\name{make_run}
\alias{make_run}
\title{Run the full crosswalk generation pipeline}
\usage{
make_run(
  output_dir = "outputs",
  auto_detect_latest = TRUE,
  preferred_cycle = NULL,
  external_data_dir = "data/external",
  buffer_feet = -50,
  min_area_final = 100,
  epsilon = 1e-06,
  exclude_ids = "cc_upcoming",
  primary_only = NULL,
  targets = NULL,
  max_primaries = NULL,
  zip_artifacts = FALSE
)
}
\arguments{
\item{output_dir}{Base directory for outputs. Default \code{"outputs"}.}

\item{auto_detect_latest}{Logical. Auto-detect latest DCP cycle. Default
\code{TRUE}.}

\item{preferred_cycle}{Optional DCP cycle letter to pin.}

\item{external_data_dir}{Path to local fallback files. Default
\code{"data/external"}.}

\item{buffer_feet}{Negative buffer for intersection de-noising. Default
\code{-50}.}

\item{min_area_final}{Minimum intersection area (sq ft). Default \code{100}.}

\item{epsilon}{Numerical noise threshold. Default \code{1e-6}.}

\item{exclude_ids}{Geography IDs to exclude. Default \code{"cc_upcoming"}.}

\item{primary_only}{Optional character vector of primary IDs to build.}

\item{targets}{Optional character vector of target IDs.}

\item{max_primaries}{Optional limit on features per primary (for testing).}

\item{zip_artifacts}{Logical. If \code{TRUE}, create ZIP archives of the
outputs. Default \code{FALSE}.}
}
\value{
The path to the timestamped run directory (invisibly).
}
\description{
Orchestrates the complete workflow: downloads all NYC boundary datasets
via \code{\link[=download_all_boundaries]{download_all_boundaries()}}, then builds crosswalk tables via
\code{\link[=build_crosswalks]{build_crosswalks()}}. Optionally creates ZIP archives of the outputs.
}
